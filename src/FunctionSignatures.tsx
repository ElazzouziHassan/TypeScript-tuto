let sayHello: (text: string) => void;

sayHello = (text: string) => {
  console.log(`${text}, World!`);
}


let addOperation: (numberOne: number, numberTwo: number) => void;
addOperation = (a: number, b: number) => {
  return a + b;
}